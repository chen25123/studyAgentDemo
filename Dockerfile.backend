FROM python:3.13-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY llm/ ./llm/
COPY migrations/ ./migrations/
COPY alembic.ini pyproject.toml ./

EXPOSE 8010

CMD ["uvicorn", "llm.api.app:app", "--host", "0.0.0.0", "--port", "8010"]
